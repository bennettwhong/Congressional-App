# Congressional App Challenge – Voice Transcriber

This project is an iOS + Flask application that records audio, sends it to a backend server, and uses OpenAI's Whisper model to transcribe it into text.
